# Git Status

Repo is initialized.
Commit: `feat: complete project setup with CI/CD and installers`
Tag: `v1.0.0`

Run these commands to push:

```bash
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
git push origin v1.0.0
```
