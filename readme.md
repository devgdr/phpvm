# PHPVM – Dockerized PHP Environment

> **فلسفه اصلی پروژه:**
> هدف PHPVM حذف "سربار یادگیری" (Overhead) ابزارهایی است که روی داکر ساخته شده‌اند.
> بسیاری از ابزارها سعی می‌کنند داکر را پشت یک CLI اختصاصی مخفی کنند و شما مجبورید دستورات عجیب و غریب آنها را یاد بگیرید.
> اما **PHPVM** متفاوت است: این ابزار تمام فایل‌های مورد نیاز (مثل `Dockerfile`, `docker-compose.yml`, `Makefile`) را مستقیماً در اختیار شما قرار می‌دهد. هیچ چیزی مخفی نیست. شما کنترل کامل روی محیط خود دارید و هر زمان بخواهید می‌توانید بدون نیاز به PHPVM پروژه را اجرا کنید.

---

## 🚀 چرا PHPVM؟

1.  **حذف لایه‌های اضافی**: به جای یادگیری دستورات خاص یک ابزار، از استاندارد `docker-compose` و `make` که همه می‌شناسند استفاده کنید.
2.  **دسترسی کامل**: فایل‌های کانفیگ (`nginx`, `php`, `mysql`) توی پروژه شما کپی می‌شوند. هر تغییری بخواهید (مثلاً نصب اکستنشن خاص) راحت انجام می‌دهید.
3.  **بدون وابستگی (Vendor Lock-in)**: اگر فردا تصمیم گرفتید PHPVM را پاک کنید، پروژه شما همچنان کار می‌کند! چون تمام فایل‌ها استاندارد هستند.
4.  **نصب آسان**: با چند کلیک یا یک دستور ساده، محیط توسعه آماده می‌شود.

---

## ⚙️ نصب (Installation)

نسخه مناسب سیستم‌عامل خود را از بخش **Releases** گیت‌هاب دانلود کنید.

### 🪟 Windows
1. فایل `phpvm-setup.exe` را دانلود و نصب کنید.
2. گزینه **"Add to system PATH"** را حتماً تیک بزنید.
3. CMD یا PowerShell را باز کنید و بنویسید: `phpvm`

### 🐧 Linux (Debian/Ubuntu)
```bash
# فایل .deb را دانلود و نصب کنید
sudo dpkg -i phpvm_1.0.1_amd64.deb

# اجرا
phpvm
```

---

## � نحوه استفاده (Usage)

1. **ساخت پروژه جدید**:
   در پوشه خالی پروژه خود دستور زیر را بزنید و به سوالات (نام پروژه، نسخه PHP) پاسخ دهید:
   ```bash
   phpvm
   ```

2. **مدیریت پروژه**:
   بعد از نصب، فایل `Makefile` به پروژه شما اضافه می‌شود. حالا می‌توانید با دستورات استاندارد کار کنید:

   - **اجرای سرویس‌ها**: `make up`
   - **ورود به شل PHP**: `make php`
   - **اجرای کامپوزر**: `make composer install`
   - **خاموش کردن**: `make down`

### مثال واقعی:
```bash
# 1. ساخت پروژه
mkdir my-app && cd my-app
phpvm

# 2. اجرای کانتینرها
make up

# 3. نصب پکیج لاراول (مثال)
make composer create-project laravel/laravel .
```

---

## 🛠 توسعه‌دهندگان (Building from Source)
اگر می‌خواهید سورس برنامه را تغییر دهید یا خودتان بیلد بگیرید، راهنمای [RELEASE.md](RELEASE.md) را بخوانید.
