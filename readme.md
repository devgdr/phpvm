# PHPVM – Dockerized PHP Environment

> این برنامه تماما با انواع هوش مصنوعی تولید شده و هدفش استفاده راحت از PHP نسخه ۸ و بالاتر با Docker است.

---

## 🚀 ویژگی‌ها

- استفاده از **Docker** برای راه‌اندازی سریع PHP ۸+ بدون نصب مستقیم روی سیستم.
- دستورات محلی **php**, **composer** مستقیماً در پوشه پروژه فعال هستند.
- **کانفیگ انعطاف‌پذیر**: می‌توانید فایل‌های پروژه را تغییر دهید و دوباره build کنید؛ برنامه انتزاع جدید نمی‌سازد.
- پورت‌های **xdebug و FPM** روی ماشین لوکال قابل دسترسی هستند.

- دستور داده شده بعد از پایان نصب رو روی شلتون اعمال کنید این یه بار نیازه.

---

## ⚙️ نصب و اجرا (Installation)

### 📥 دانلود (Download)
از بخش **Releases** گیت‌هاب، فایل مناسب سیستم‌عامل خود را دانلود کنید:

### 🪟 Windows
1. فایل `phpvm-setup.exe` را دانلود و اجرا کنید.
2. در مراحل نصب، تیک گزینه **"Add to system PATH"** را حتماً بزنید.
3. ترمینال (CMD/PowerShell) را باز کنید و دستور `phpvm` را اجرا کنید.

### 🐧 Linux (Debian/Ubuntu)
```bash
# دانلود فایل .deb و نصب آن
sudo dpkg -i phpvm_1.0.0_amd64.deb
```

## 🛠 ساخت دستی (Optional - Build from Source)
اگر می‌خواهید خودتان برنامه را بیلد کنید، فایل [RELEASE.md](RELEASE.md) را مطالعه کنید.
